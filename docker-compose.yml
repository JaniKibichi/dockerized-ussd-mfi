version: "2"
services: 
  db: 
    build: "./Database/"
    restart: always

  ussd: 
    build: "."
    command: "/usr/bin/hhvm -m server -p 6500"
    depends_on: 
    ports: 
      - "6500:6500"
    restart: always


